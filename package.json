{
  "name": "simple-appserver",
  "private": true,
  "scripts": {
    "build": "turbo build  --filter !date-test",
    "dev": "turbo dev --filter !date-test",
    "start": "turbo start --filter !date-test",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "turbo": "^2.3.3",
    "typescript": "5.5.4"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
